/*====================================================================================
 *
 * Copyright (C) Ruvendix. All Rights Reserved.
 *
 * <작성 날짜>
 * 2018-08-08 (AssortRock 전용으로 수정)
 *
 * <파일 내용>
 * 공동으로 사용되는 전역 형식 모음입니다.
 * 구조체, 공용체 등이 있습니다.
 * 미리 컴파일된 헤더파일에 포함됩니다.
 *
 ====================================================================================*/
#ifndef GLOBAL_TYPE_H__
#define GLOBAL_TYPE_H__

// ====================================================================================
// 사용자 재정의 자료형입니다.
// ====================================================================================
using FLOAT32 = float;
using FLOAT64 = double;

// ====================================================================================
// 함수 포인터 형식입니다.
// ====================================================================================


// ====================================================================================
// 공용체입니다.
// ====================================================================================
enum class PROJECT_MODE : INT32
{
	DEBUG = 0,
	RELEASE,
};

// 함수 단위로 반환 상태를 판단할 때 사용됩니다.
enum class ROUTINE_RETURN_TYPE : INT32
{
	SUCCESS = 0,
	FAILURE,
	EXIT,
};

enum class FPS_TYPE : INT32
{
	THIRTY,
	SIXTY,
	UNLIMITEDNESS,
};

// ====================================================================================
// 구조체입니다.
// ====================================================================================
struct ResolutionInfo
{
	ResolutionInfo()
	{
		width  = 0;
		height = 0;
	}

	INT32 width;
	INT32 height;
};

#endif